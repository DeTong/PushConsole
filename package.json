{
  "name": "apple-notifications-test",
  "version": "1.0.0",
  "description": "iOS 推送功能测试后台",
  "main": "server.js",
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider node server.js",
    "dev": "NODE_OPTIONS=--openssl-legacy-provider node server.js"
  },
  "dependencies": {
    "apn": "^2.2.0",
    "express": "^4.18.2"
  }
}
